function startContent() {
  showLyrics();

  const texts = document.querySelectorAll("p");
  texts.forEach((el, i) => {
    setTimeout(() => {
      el.style.opacity = "1";
      el.style.transform = "translateY(0)";
    }, i * 900);
  });
}
